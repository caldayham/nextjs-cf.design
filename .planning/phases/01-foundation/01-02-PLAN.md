---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/ui/Icons.tsx
  - components/ui/Reveal.tsx
  - components/layout/Nav.tsx
  - components/layout/Footer.tsx
  - components/layout/SiteShell.tsx
  - app/layout.tsx
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06

must_haves:
  truths:
    - "Visiting / shows a nav bar with logo 'cf.design', menu items, case study dropdown, and 'Start Project' button"
    - "Visiting / shows a footer with location, phone, email, Instagram, Nextdoor links, and copyright"
    - "Scrolling past 50px transitions the nav from transparent to frosted dark background"
    - "All 16 SVG icons render correctly when used in Nav and Footer"
    - "Content wrapped in Reveal component fades in when scrolled into view"
  artifacts:
    - path: "components/ui/Icons.tsx"
      provides: "16 SVG icon components (Menu, X, ArrowRight, ArrowDown, ArrowLeft, Check, MapPin, Mail, ChevronDown, Layout, PenTool, FileText, Hammer, Smile, Instagram, Home)"
      exports: ["Menu", "X", "ArrowRight", "ArrowDown", "ArrowLeft", "Check", "MapPin", "Mail", "ChevronDown", "Layout", "PenTool", "FileText", "Hammer", "Smile", "Instagram", "Home"]
    - path: "components/ui/Reveal.tsx"
      provides: "IntersectionObserver fade-in wrapper with configurable delay"
      exports: ["Reveal"]
    - path: "components/layout/Nav.tsx"
      provides: "Fixed nav with scroll-based style transition, mobile menu, case study dropdown"
      contains: "use client"
    - path: "components/layout/Footer.tsx"
      provides: "Footer with contact info, social links, copyright"
      contains: "MapPin"
    - path: "components/layout/SiteShell.tsx"
      provides: "Client wrapper managing inquiry modal state, renders Nav + children + Footer"
      contains: "use client"
    - path: "app/layout.tsx"
      provides: "Root layout wrapping children in SiteShell"
      contains: "SiteShell"
  key_links:
    - from: "app/layout.tsx"
      to: "components/layout/SiteShell.tsx"
      via: "import and wrapping children"
      pattern: "import SiteShell"
    - from: "components/layout/SiteShell.tsx"
      to: "components/layout/Nav.tsx"
      via: "import and render with onInquiry prop"
      pattern: "import Nav"
    - from: "components/layout/SiteShell.tsx"
      to: "components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import Footer"
    - from: "components/layout/Nav.tsx"
      to: "components/ui/Icons.tsx"
      via: "import Menu, X, ChevronDown icons"
      pattern: "import.*Icons"
    - from: "components/layout/Nav.tsx"
      to: "data/case-studies.ts"
      via: "import CASE_STUDIES for dropdown"
      pattern: "import.*case-studies"
    - from: "components/layout/Footer.tsx"
      to: "components/ui/Icons.tsx"
      via: "import MapPin, Mail, Instagram, Home icons"
      pattern: "import.*Icons"
---

<objective>
Port all shared components from the existing site into the Next.js project: Nav, Footer, SiteShell, Reveal, and all 16 SVG icons.

Purpose: These components form the persistent layout shell visible on every page. They must render identically to the current site before any page-specific content (Phase 2+) is built on top.

Output: A working layout shell with fixed navigation (scroll-based transition), footer with contact info, SiteShell client wrapper for future modal state, Reveal animation component, and all SVG icons.
</objective>

<execution_context>
@/Users/caldayham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caldayham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 artifacts — executor needs these contracts -->

From app/layout.tsx (created by Plan 01):
```tsx
// Server Component — root layout
// Font variables --font-inter and --font-playfair applied to <html>
// Body renders {children} directly — Plan 02 wraps in SiteShell
import './globals.css'
```

From app/globals.css (created by Plan 01):
```css
/* Available Tailwind classes from @theme inline: */
/* Colors: bg-arch-black, bg-arch-charcoal, bg-arch-mineral, bg-arch-stone, bg-arch-concrete, bg-arch-white, bg-arch-bronze */
/* Fonts: font-sans (Inter), font-serif (Playfair Display) */
/* Animations: animate-slow-zoom, animate-fade-in, animate-fade-in-fast, animate-scroll-up, animate-scroll-down, animate-scroll-left, animate-float */
/* Utility classes: .reveal-base, .no-scrollbar, .hover-pause */
```

From data/case-studies.ts (created by Plan 01):
```typescript
export interface CaseStudy {
  title: string
  href: string
}
export const CASE_STUDIES: CaseStudy[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Icons and Reveal components</name>
  <files>components/ui/Icons.tsx, components/ui/Reveal.tsx</files>
  <action>
1. Create `components/ui/Icons.tsx` — Server Component (no 'use client'). Port ALL 16 SVG icons verbatim from 01-RESEARCH.md Pattern 7.

Include the base `Icon` wrapper component with these exact SVG defaults:
- `xmlns="http://www.w3.org/2000/svg"`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth="1.5"`, `strokeLinecap="square"`, `strokeLinejoin="miter"`
- Accept `className`, `size` (default 24), and rest props via `IconProps` interface

Export ALL 16 named icons: Menu, X, ArrowRight, ArrowDown, ArrowLeft, Check, MapPin, Mail, ChevronDown, Layout, PenTool, FileText, Hammer, Smile, Instagram, Home.

EXACT SVG paths for each icon are in 01-RESEARCH.md Pattern 7. Copy verbatim — do not use a different icon library.

2. Create `components/ui/Reveal.tsx` — Client Component ('use client'). Port the Reveal component verbatim from 01-RESEARCH.md Pattern 6.

Key implementation details:
- `useRef<HTMLDivElement>` for the target element
- `IntersectionObserver` with `threshold: 0.1` in a `useEffect`
- `observer.unobserve(entry.target)` after first intersection (fire once)
- Hidden state: `opacity-0 translate-y-12` (starts 3rem below)
- Visible state: `opacity-100 translate-y-0`
- Uses `.reveal-base` CSS class for transition easing (defined in globals.css)
- `delay` prop in milliseconds applied via inline `style={{ transitionDelay: '${delay}ms' }}`
- Accept `children`, `delay` (default 0), `className` (default '')
- Export as named export: `export function Reveal(...)`
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/v3.cf.design && grep -c "export const" components/ui/Icons.tsx && grep "IntersectionObserver" components/ui/Reveal.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Icons.tsx exports exactly 16 named icon components with correct SVG paths
- Reveal.tsx is a 'use client' component using IntersectionObserver with threshold 0.1, fires once, applies reveal-base class
- `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Nav, Footer, SiteShell components and wire into layout</name>
  <files>components/layout/Nav.tsx, components/layout/Footer.tsx, components/layout/SiteShell.tsx, app/layout.tsx</files>
  <action>
1. Create `components/layout/Nav.tsx` — Client Component ('use client'). Port from 01-RESEARCH.md Pattern 4.

MUST implement these behaviors:
- **Scroll detection:** `useEffect` with `window.addEventListener('scroll', ...)` — `setScrolled(window.scrollY > 50)`. Clean up listener on unmount.
- **Style transition:** When `scrolled=false`: `bg-transparent py-4 md:py-6`. When `scrolled=true`: `bg-arch-black/90 backdrop-blur-md border-b border-white/10` (remove extra padding).
- **Safe area:** `style={{ paddingTop: 'env(safe-area-inset-top, 0px)' }}` on `<nav>`.
- **Logo:** Use `usePathname()` from `next/navigation`. On `/` → `<span>` with onClick scrolling to top. On other routes → `<Link href="/">`. Logo text: "cf.design" in `text-sm font-bold tracking-wider text-white`.
- **Desktop nav items:** hash links `#purpose`, `#process`, `#portfolio`, `#reviews`, `#faq` — styled `text-xs font-bold tracking-[0.15em] text-white/70 hover:text-white uppercase`.
- **Case study dropdown:** Desktop: `onMouseEnter`/`onMouseLeave` on wrapper div with `min-w-[310px]` dropdown. Contains "Case Studies" trigger with ChevronDown icon, dropdown lists CASE_STUDIES items. Import from `@/data/case-studies`.
- **Mobile menu:** Hamburger (Menu icon) toggles full-screen white overlay. Links in `font-serif text-3xl`. "Inquiries" text calls `onInquiry`. Mobile case study section toggles separately.
- **"Start Project" button:** Desktop only. `border border-white text-white hover:bg-white hover:text-arch-black` — calls `onInquiry` prop.
- **Props:** `interface NavProps { onInquiry: () => void }`

2. Create `components/layout/Footer.tsx` — Server Component (no 'use client'). Port from 01-RESEARCH.md Pattern 5.

MUST include:
- Root: `<footer className="bg-arch-black text-arch-stone/40 py-12 border-t border-white/10 text-xs tracking-widest uppercase px-6">`
- Location: MapPin icon + "Serving San Mateo & Santa Clara County"
- Contact: SMS link `sms:+16505217269&body=I'm%20interested%20in%20` with text "(650) 521-7269", email link `mailto:info@cf.design` with Mail icon
- Social: Instagram link `https://www.instagram.com/cf.design__/` with Instagram icon, Nextdoor link `https://nextdoor.com/page/cf-design` with Home icon
- Copyright: "© 2026 Cal and Fynn Design Services" + cf-icon.png at `opacity-50 grayscale`
- All links: `hover:text-white transition-colors`

3. Create `components/layout/SiteShell.tsx` — Client Component ('use client'). Port from 01-RESEARCH.md Pattern 3.

MUST implement:
- `const [showInquiry, setShowInquiry] = useState(false)`
- `const openInquiry = () => setShowInquiry(true)`
- `const closeInquiry = () => setShowInquiry(false)`
- Render: `<div className="font-sans text-arch-charcoal bg-arch-concrete">` wrapping Nav + children + Footer
- Pass `onInquiry={openInquiry}` to Nav
- Leave a comment `{/* InquiryModal mounted here in Phase 3 */}` for future modal placement
- Accept `children: React.ReactNode` prop

4. Update `app/layout.tsx` to import and wrap children with SiteShell:
- Add `import SiteShell from '@/components/layout/SiteShell'`
- Wrap `{children}` in `<SiteShell>{children}</SiteShell>` inside the `<body>` tag
- Keep all existing font imports, metadata, className on html/body unchanged
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/v3.cf.design && grep "use client" components/layout/Nav.tsx && grep "use client" components/layout/SiteShell.tsx && grep "SiteShell" app/layout.tsx && grep "MapPin" components/layout/Footer.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Nav.tsx is 'use client', accepts onInquiry prop, transitions at scrollY > 50, has mobile menu, case study dropdown, logo with pathname-aware behavior
- Footer.tsx is a Server Component with MapPin, Mail, Instagram, Home icons, all correct links and contact info
- SiteShell.tsx is 'use client', holds showInquiry state, renders Nav (with onInquiry) + children + Footer
- layout.tsx imports SiteShell and wraps children
- `npm run build` succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify layout shell renders correctly</name>
  <files>components/layout/Nav.tsx, components/layout/Footer.tsx, components/layout/SiteShell.tsx, components/ui/Icons.tsx, components/ui/Reveal.tsx, app/layout.tsx</files>
  <action>
Human verification checkpoint. All implementation is complete in Tasks 1-2. Run `npm run dev` and verify the layout shell visually:

1. Visit `http://localhost:3000`
2. **Nav:** Logo "cf.design" top-left in white, menu items (Purpose, Process, Portfolio, Reviews, FAQ) in uppercase, "Start Project" button with white border, "Case Studies" dropdown on hover showing both case studies
3. **Scroll:** At top = transparent nav. Scroll past ~50px = dark frosted background transition
4. **Mobile (resize to less than 768px):** Hamburger icon appears, tapping opens white overlay with serif font links, "Inquiries" text in mobile menu
5. **Footer (scroll to bottom):** Dark background, "Serving San Mateo and Santa Clara County", phone/email links, Instagram/Nextdoor links, copyright
6. **Fonts:** Heading in serif (Playfair Display), body text in sans-serif (Inter)
7. **Colors:** Warm off-white background (arch-concrete), color swatches show mineral (green), bronze (brown), stone (light gray), charcoal (dark)
8. **404:** Visit `/nonexistent` — branded page with "Page Not Found" in serif, "Back Home" button

Type "approved" or describe issues to continue.
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/v3.cf.design && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms nav, footer, fonts, colors, scroll transition, mobile menu, and 404 page all render correctly and match current site appearance</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 6 component files exist: Icons.tsx, Reveal.tsx, Nav.tsx, Footer.tsx, SiteShell.tsx
3. layout.tsx imports and uses SiteShell wrapper
4. Nav transitions from transparent to frosted at scrollY > 50
5. Footer renders all contact info and social links
6. 16 SVG icons render correctly in Nav and Footer
7. Human confirms visual appearance matches current site's nav and footer
</verification>

<success_criteria>
- All 16 SVG icons export as named React components from Icons.tsx
- Reveal component triggers once-only fade-in animation on scroll
- Nav has working scroll detection (50px threshold), mobile menu toggle, case study dropdown
- Footer displays correct contact info, social links, and copyright
- SiteShell wraps Nav + children + Footer with inquiry modal state management
- layout.tsx uses SiteShell as the child wrapper
- Human approves visual fidelity of nav and footer against current site
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
