---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/not-found.tsx
  - data/case-studies.ts
  - public/assets/
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "Running `npm run dev` starts a Next.js 16 dev server without errors"
    - "Visiting localhost shows a page styled with arch-* colors (arch-concrete background, arch-black text)"
    - "Inter and Playfair Display fonts load from self-hosted files (no Google Fonts CDN requests in Network tab)"
    - "All images under /assets/ return 200 (no 404s in browser network tab)"
    - "Navigating to /nonexistent shows a branded 404 page with arch-* styling"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme block with all arch-* colors, 7 animations, keyframes, utility classes"
      contains: "@theme inline"
    - path: "app/layout.tsx"
      provides: "Root layout with next/font Inter + Playfair Display, metadata, scroll-smooth html"
      contains: "next/font/google"
    - path: "app/page.tsx"
      provides: "Homepage stub rendering visible content"
      min_lines: 5
    - path: "app/not-found.tsx"
      provides: "Branded 404 page"
      contains: "Page Not Found"
    - path: "data/case-studies.ts"
      provides: "CASE_STUDIES array for Nav dropdown"
      exports: ["CASE_STUDIES", "CaseStudy"]
    - path: "public/assets/cf-icon.png"
      provides: "Favicon asset exists in public directory"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "app/globals.css"
      to: "Tailwind v4"
      via: "@import 'tailwindcss' and @theme inline block"
      pattern: "@import.*tailwindcss"
    - from: "app/layout.tsx"
      to: "next/font/google"
      via: "Inter and Playfair_Display font variables on <html>"
      pattern: "inter\\.variable.*playfair\\.variable"
---

<objective>
Scaffold the Next.js 16 App Router project and establish the complete design system foundation.

Purpose: Every subsequent component depends on design tokens (arch-* colors), fonts (Inter, Playfair Display), and asset paths being correct. This plan resolves all scaffold dependencies before any component is written.

Output: A running Next.js dev server with correct Tailwind v4 config, self-hosted fonts, all 79+ image assets migrated, case study data module, and a branded 404 page.
</objective>

<execution_context>
@/Users/caldayham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caldayham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with Tailwind v4 design tokens and self-hosted fonts</name>
  <files>next.config.ts, tsconfig.json, postcss.config.mjs, app/globals.css, app/layout.tsx, app/page.tsx</files>
  <action>
1. Run `npx create-next-app@latest . --typescript --tailwind --app --turbopack --yes` from the project root to scaffold in-place. If it prompts about existing files, allow overwrite of config files but preserve the existing `assets/` directory and `index.html`.

2. Update `next.config.ts` to:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  trailingSlash: true,
}

export default nextConfig
```
Note: `trailingSlash: true` preserves existing URL structure (case study URLs have trailing slashes in current site).

3. Replace the generated `app/globals.css` with the COMPLETE design token file. Use `@theme inline` for the full block. Include:
- `@import 'tailwindcss';`
- `@theme inline { }` with ALL arch-* colors (black #121212, charcoal #1A1A1A, mineral #4A5D4F, stone #E5E4E2, concrete #F5F5F4, white #FFFFFF, bronze #8C7A6B), font-sans/font-serif variables, ALL 7 animation tokens (slow-zoom, fade-in, fade-in-fast, scroll-up, scroll-down, scroll-left, float), bg-noise background-image
- ALL 6 `@keyframes` blocks OUTSIDE @theme: zoom, fadeIn, scrollUp, scrollDown, scrollLeft, float
- Global utility classes: `.no-scrollbar`, `.reveal-base` (transition: all 1s cubic-bezier(0.16, 1, 0.3, 1)), `.hover-pause`, body defaults (bg #F5F5F4, color #252525), select option styles

EXACT values are in 01-RESEARCH.md Pattern 1. Copy verbatim.

4. Replace `app/layout.tsx` with the root layout. Include:
- `import { Inter, Playfair_Display } from 'next/font/google'`
- Inter: subsets ['latin'], weight ['300', '400', '500', '600'], display 'swap', variable '--font-inter'
- Playfair_Display: subsets ['latin'], weight ['400', '500', '600', '700'], style ['normal', 'italic'], display 'swap', variable '--font-playfair'
- Metadata: title "Custom local quality construction!", description "Constructing beautiful solutions in San Mateo & Santa Clara County.", icons: { icon: '/assets/cf-icon.png' }
- `<html lang="en" className="scroll-smooth ${inter.variable} ${playfair.variable}">`
- `<body className="antialiased overflow-x-hidden selection:bg-arch-mineral selection:text-white">`
- Body renders `{children}` directly (SiteShell wrapper added in Plan 02)

EXACT pattern is in 01-RESEARCH.md "Root Layout" code example. Note: Do NOT import SiteShell yet -- that happens in Plan 02.

5. Replace `app/page.tsx` with a minimal homepage stub that visually confirms tokens work:
```tsx
export default function Home() {
  return (
    <main className="min-h-screen bg-arch-concrete flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-serif text-arch-black mb-4">cf.design</h1>
        <p className="text-arch-charcoal/70 font-light">Foundation scaffold — Phase 1</p>
        <div className="mt-8 flex gap-4 justify-center">
          <div className="w-8 h-8 rounded bg-arch-mineral" title="mineral" />
          <div className="w-8 h-8 rounded bg-arch-bronze" title="bronze" />
          <div className="w-8 h-8 rounded bg-arch-stone" title="stone" />
          <div className="w-8 h-8 rounded bg-arch-charcoal" title="charcoal" />
        </div>
      </div>
    </main>
  )
}
```
This is a TEMPORARY stub page replaced in Phase 2. Its purpose is to visually verify colors and fonts work.

6. Run `npm run dev` and verify the dev server starts without errors. Check the terminal output for any Tailwind or font compilation errors.
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/v3.cf.design && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- `npm run build` succeeds with zero errors
- globals.css contains @theme inline block with all 7 arch-* colors, 7 animations, and bg-noise
- layout.tsx imports Inter and Playfair_Display from next/font/google with correct weights
- page.tsx renders a stub using arch-* color classes and font-serif
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate assets, create case study data module, and build branded 404 page</name>
  <files>public/assets/, data/case-studies.ts, app/not-found.tsx</files>
  <action>
1. Copy all assets from the existing `assets/` directory to `public/assets/`:
```bash
cp -r assets/ public/assets/
```
Verify the structure matches:
- `public/assets/cf-icon.png` (favicon)
- `public/assets/cal-fynn-build.jpg`, `cal-fynn-outdoor-design.jpg`, `diagram_collage.jpg`, `Day-ham-family.jpg`, `Fynn-workshop.jpg`, `suzi-build-process.jpg`
- `public/assets/jobs/` (38+ images)
- `public/assets/jobs/mobile/` (21 mobile variants)
- `public/assets/reviews/` (17 review screenshots)
- `public/assets/mobile/` (1 file: cal-fynn-outdoor-design.jpg)

2. Create `data/case-studies.ts`:
```typescript
export interface CaseStudy {
  title: string
  href: string
}

export const CASE_STUDIES: CaseStudy[] = [
  { title: "Perry's Little Library", href: '/case-studies/palo-alto-redwood-little-library' },
  { title: "Michelle's Walnut Tables", href: '/case-studies/palo-alto-walnut-marble-tables' },
]
```
Note: No trailing slashes on hrefs -- Next.js `trailingSlash: true` in next.config handles this automatically.

3. Create `app/not-found.tsx` — branded 404 page:
```tsx
import Link from 'next/link'

export default function NotFound() {
  return (
    <div className="min-h-screen bg-arch-concrete flex items-center justify-center px-6">
      <div className="text-center">
        <p className="text-xs font-bold tracking-[0.25em] text-arch-mineral uppercase mb-4">404</p>
        <h1 className="text-4xl md:text-6xl font-serif text-arch-black mb-6">Page Not Found</h1>
        <p className="text-arch-charcoal/70 mb-10 font-light">
          This page doesn&apos;t exist or has been moved.
        </p>
        <Link href="/" className="inline-flex items-center gap-3 bg-arch-black text-white px-8 py-3 text-xs font-bold tracking-[0.15em] uppercase hover:bg-arch-charcoal transition-colors">
          Back Home
        </Link>
      </div>
    </div>
  )
}
```

4. Verify assets are accessible by checking that key files exist in public/assets/:
```bash
ls public/assets/cf-icon.png public/assets/jobs/ public/assets/reviews/ public/assets/mobile/
```
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/v3.cf.design && test -f public/assets/cf-icon.png && test -d public/assets/jobs && test -d public/assets/reviews && test -f data/case-studies.ts && test -f app/not-found.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- All image assets exist under public/assets/ with correct subdirectory structure (jobs/, jobs/mobile/, reviews/, mobile/)
- data/case-studies.ts exports CASE_STUDIES array with 2 entries and CaseStudy interface
- app/not-found.tsx exists with branded 404 UI using arch-* colors and font-serif
- `npm run build` still succeeds after adding all files
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -c 'arch-' app/globals.css` returns 7+ (all color tokens present)
3. `grep 'next/font/google' app/layout.tsx` confirms font imports
4. `ls public/assets/cf-icon.png` confirms asset migration
5. `grep 'Page Not Found' app/not-found.tsx` confirms 404 page exists
</verification>

<success_criteria>
- Next.js 16 project builds and runs with zero errors
- All 7 arch-* colors defined in @theme inline block
- Inter (300-600) and Playfair Display (400-700 + italic) self-hosted via next/font
- 79+ image assets accessible at /assets/* paths
- Branded 404 page renders at non-existent routes
- Case study data module exports CASE_STUDIES array
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
