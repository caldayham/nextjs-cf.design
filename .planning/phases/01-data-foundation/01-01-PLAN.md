---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/cities.ts
  - data/services.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "15 Peninsula cities are defined with slugs, display names, neighborhoods, county, and geographic characteristics"
    - "11 service specialties are defined with slugs, keywords, categories, and long descriptions"
    - "City and service data export lookup helpers (by-slug Maps, slug arrays) for downstream consumers"
    - "Existing specialties.ts is unchanged — new services.ts is a separate enriched file"
  artifacts:
    - path: "data/cities.ts"
      provides: "City interface, CITIES array (15 items), CITY_BY_SLUG Map, CITY_SLUGS array, CitySlug type"
      exports: ["City", "CITIES", "CITY_BY_SLUG", "CITY_SLUGS", "CitySlug"]
    - path: "data/services.ts"
      provides: "Service interface, SERVICES array (11 items), SERVICE_BY_SLUG Map, SERVICE_SLUGS array, ServiceSlug type"
      exports: ["Service", "SERVICES", "SERVICE_BY_SLUG", "SERVICE_SLUGS", "ServiceSlug"]
  key_links:
    - from: "data/services.ts"
      to: "data/specialties.ts"
      via: "slug alignment — service slugs match specialty href segments"
      pattern: "slug values match /specialties/{slug} hrefs"
---

<objective>
Create the foundational city and service data files that all other Phase 1 data (local knowledge, case study proximity, keywords) depends on.

Purpose: Cities and services are the two axes of the PSEO system (15 cities x 11 services = 165 pages). Every downstream data file and every page route depends on these slugs and types.
Output: `data/cities.ts` with 15 cities, `data/services.ts` with 11 enriched services.
</objective>

<execution_context>
@/Users/caldayham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caldayham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing data file pattern to follow -->

From data/specialties.ts:
```typescript
export interface Specialty {
  title: string
  href: string
  description: string
}

export const SPECIALTIES: Specialty[] = [
  {
    title: 'Garden Boxes',
    href: '/specialties/garden-boxes',
    description: 'Custom raised garden beds and planter boxes built to last.',
  },
  // ... 10 more
]
```

From data/case-studies.ts:
```typescript
export interface CaseStudy {
  title: string
  href: string
  image: string
  location: string
  description: string
}
```

<!-- Service slugs MUST align with existing specialty hrefs -->
Specialty hrefs (extract slug from path):
- garden-boxes
- tree-shrub-removal
- shed-renovation
- fences-gates-decks
- custom-carpentry
- hardscape
- painting
- squirrel-rat-excluders
- refinishing
- landscape-design
- demolition-hauling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create city data file with 15 Peninsula cities</name>
  <files>data/cities.ts</files>
  <action>
Create `data/cities.ts` with:

1. **City interface:**
```typescript
export interface City {
  slug: string
  name: string
  county: 'San Mateo' | 'Santa Clara'
  neighborhoods: string[]
  characteristics: string[]
  lat: number
  lng: number
}
```

2. **CITIES array** with these 15 cities (in geographic order, north to south):
- Burlingame (San Mateo) — neighborhoods: Broadway, Easton Addition, Mills Estate, Ray Park; characteristics: tree-lined streets, 1920s-1940s homes, hillside properties
- San Mateo (San Mateo) — neighborhoods: Hillsdale, Baywood, San Mateo Park, Aragon; characteristics: diverse housing stock, downtown core, Central Park area
- Foster City (San Mateo) — neighborhoods: Foster City (planned community), Pilgrim Triton; characteristics: planned waterfront community, lagoon system, newer construction
- Belmont (San Mateo) — neighborhoods: Belmont Hills, Hallmark, Sterling Downs; characteristics: hillside homes, canyon properties, mid-century ranches
- San Carlos (San Mateo) — neighborhoods: White Oaks, Devonshire, Alder Manor, Howard Park; characteristics: Laurel Street downtown, craftsman homes, hill properties
- Redwood City (San Mateo) — neighborhoods: Emerald Hills, Woodside Plaza, Farm Hill, Edgewood Park area; characteristics: downtown renaissance, diverse neighborhoods, climate microzone
- Hillsborough (San Mateo) — neighborhoods: (no distinct neighborhoods — single affluent residential town); characteristics: estate properties, large lots, gated drives, no commercial district
- Atherton (San Mateo) — neighborhoods: Lindenwood, West Atherton, Atherton Oaks; characteristics: estate properties, equestrian trails, no sidewalks in many areas, gated estates
- Menlo Park (San Mateo/Santa Clara) — neighborhoods: Allied Arts, The Willows, Sharon Heights, Belle Haven, Suburban Park; characteristics: mix of tech campus area and residential, Stanford Research Park proximity
- Woodside (San Mateo) — neighborhoods: Woodside Hills, Skywood/Skylonda, Kings Mountain; characteristics: rural-feel properties, horse country, redwood groves, winding roads
- Portola Valley (San Mateo) — neighborhoods: Portola Valley Ranch, Westridge, Blue Oaks; characteristics: open space preserves, rural residential, equestrian, oak woodlands
- Palo Alto (Santa Clara) — neighborhoods: Old Palo Alto, Crescent Park, Barron Park, College Terrace, Professorville, Midtown, Greenmeadow; characteristics: historic craftsman homes, mature oak canopy, Stanford University proximity
- Los Altos (Santa Clara) — neighborhoods: Country Club, North Los Altos, Loyola Corners, Old Los Altos; characteristics: suburban residential, orchard heritage, downtown village feel
- Los Altos Hills (Santa Clara) — neighborhoods: (rural residential — no distinct named neighborhoods); characteristics: large lots (1+ acre minimum), hillside properties, horse country, limited street lighting
- Mountain View (Santa Clara) — neighborhoods: Old Mountain View, Cuesta Park, Rex Manor, Waverly Park, Gemello; characteristics: tech campus proximity, Castro Street downtown, diverse housing mix

3. **Lookup helpers:**
```typescript
export const CITY_BY_SLUG = new Map(CITIES.map(c => [c.slug, c]))
export const CITY_SLUGS = CITIES.map(c => c.slug) as readonly string[]
export type CitySlug = typeof CITIES[number]['slug']
```

Use lat/lng approximate center coordinates for each city. These are for potential future distance calculations, not display.

IMPORTANT: Do NOT modify `data/specialties.ts`. Leave it untouched.
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/nextjs-cf.design && npx tsc --noEmit data/cities.ts 2>&1 | head -20 && node -e "const c = require('./data/cities.ts'); console.log('ERROR: cannot require ts directly')" 2>&1 || npx tsx -e "import { CITIES, CITY_BY_SLUG, CITY_SLUGS } from './data/cities'; console.log('Cities:', CITIES.length); console.log('Slugs:', CITY_SLUGS.length); console.log('Map size:', CITY_BY_SLUG.size); if (CITIES.length !== 15) throw new Error('Expected 15 cities, got ' + CITIES.length); console.log('PASS: 15 cities defined')"</automated>
  </verify>
  <done>data/cities.ts exports City interface, CITIES array with exactly 15 Peninsula cities, CITY_BY_SLUG Map, CITY_SLUGS array, and CitySlug type. Each city has slug, name, county, neighborhoods, characteristics, lat, lng. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create enriched service data file with 11 specialties</name>
  <files>data/services.ts</files>
  <action>
Create `data/services.ts` with:

1. **Service interface:**
```typescript
export interface Service {
  slug: string
  title: string
  href: string
  description: string
  longDescription: string
  category: 'carpentry' | 'outdoor' | 'maintenance' | 'design'
  keywords: string[]
  relatedServices: string[]
}
```

2. **SERVICES array** with all 11 specialties. Slugs MUST match the path segments from existing `specialties.ts` hrefs:

| Specialty | slug | category |
|-----------|------|----------|
| Garden Boxes | garden-boxes | outdoor |
| Tree & Shrub Removal | tree-shrub-removal | outdoor |
| Shed Renovation | shed-renovation | maintenance |
| Fences, Gates & Decks | fences-gates-decks | outdoor |
| Custom Carpentry | custom-carpentry | carpentry |
| Hardscape | hardscape | outdoor |
| Painting | painting | maintenance |
| Squirrel & Rat Excluders | squirrel-rat-excluders | maintenance |
| Refinishing | refinishing | maintenance |
| Landscape Design | landscape-design | design |
| Demolition & Hauling | demolition-hauling | maintenance |

For each service:
- `title`: Match exactly from specialties.ts
- `href`: Match exactly from specialties.ts (e.g., `/specialties/garden-boxes`)
- `description`: Match exactly from specialties.ts
- `longDescription`: Write 1-2 sentences expanding on the service — what materials, what the process involves, what makes cf.design's approach distinctive. Keep it factual and specific to Peninsula carpentry work.
- `keywords`: 4-6 search keywords for this service (e.g., for garden-boxes: `['raised garden bed', 'planter box', 'garden box', 'raised bed installation', 'custom planter']`)
- `relatedServices`: 2-3 slugs of related services

3. **Lookup helpers:**
```typescript
export const SERVICE_BY_SLUG = new Map(SERVICES.map(s => [s.slug, s]))
export const SERVICE_SLUGS = SERVICES.map(s => s.slug) as readonly string[]
export type ServiceSlug = typeof SERVICES[number]['slug']
```

IMPORTANT: Do NOT modify `data/specialties.ts`. The new `services.ts` is a separate, enriched file for PSEO use.
  </action>
  <verify>
    <automated>cd /Users/caldayham/Desktop/cf.design/nextjs-cf.design && npx tsx -e "import { SERVICES, SERVICE_BY_SLUG, SERVICE_SLUGS } from './data/services'; console.log('Services:', SERVICES.length); console.log('Slugs:', SERVICE_SLUGS.length); console.log('Map size:', SERVICE_BY_SLUG.size); if (SERVICES.length !== 11) throw new Error('Expected 11 services, got ' + SERVICES.length); const cats = new Set(SERVICES.map(s => s.category)); console.log('Categories:', [...cats]); SERVICES.forEach(s => { if (!s.keywords.length) throw new Error('Missing keywords for ' + s.slug); if (!s.relatedServices.length) throw new Error('Missing relatedServices for ' + s.slug); }); console.log('PASS: 11 services with keywords and related services')"</automated>
  </verify>
  <done>data/services.ts exports Service interface, SERVICES array with exactly 11 specialties, SERVICE_BY_SLUG Map, SERVICE_SLUGS array, and ServiceSlug type. Each service has slug, title, href, description, longDescription, category, keywords, and relatedServices. Slugs align with existing specialties.ts hrefs. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors (both new files compile cleanly)
2. CITIES array has exactly 15 entries, all with unique slugs
3. SERVICES array has exactly 11 entries, slugs match specialties.ts href segments
4. `data/specialties.ts` is unchanged (verify with `git diff data/specialties.ts` showing no changes)
5. Both files export lookup Maps and slug arrays
</verification>

<success_criteria>
- 15 Peninsula cities defined with all required fields (slug, name, county, neighborhoods, characteristics, lat, lng)
- 11 services defined with enriched fields (slug, title, href, description, longDescription, category, keywords, relatedServices)
- Type exports (CitySlug, ServiceSlug) available for downstream type-safety
- Lookup helpers (CITY_BY_SLUG, SERVICE_BY_SLUG) available for O(1) access
- Existing specialties.ts untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
